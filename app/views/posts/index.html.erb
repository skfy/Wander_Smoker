<div class="container">
 <div class="row">
  <div class="col">
<h4>＜投稿一覧画面＞</h4>
<h4>投稿フォーム</h4>
<%= form_with model: @post, local:true do |f| %>
  <h4>画像</h4>
  <%= f.attachment_field :image %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>コメント</h4>
  <%= f.text_area :comment %>
  <%= f.submit '投稿' %>
<% end %>
  </div>
 </div>
</div>

<h2>map</h2>
<p>住所や駅名、目印などで検索できます。</p>
<form onsubmit="return false;">
 <input id="address" type="textbox" value="市川市">
 <input type="button" value="検索" id="map_button" onclick="codeAddress()">
</form>
<!-- 下の1行を追加 -->
<div id="display">何かが表示される、、、、！</div>

<div id='map'></div>

<style>
#map {
  height: 600px;
  width: 1200px;
}
</style>

<script>
let map
let geocoder
// 下の1行を追加
const display = document.getElementById('display')

function initMap(){
  geocoder = new google.maps.Geocoder()

  map = new google.maps.Map(document.getElementById('map'), {
    center: {lat: 35.661777, lng: 139.704051},
    zoom: 12,
  });

  marker = new google.maps.Marker({
    position:  {lat: 35.661777, lng: 139.704051},
    map: map
  });
}

function codeAddress(){
  let inputAddress = document.getElementById('address').value;

  geocoder.geocode( { 'address': inputAddress}, function(results, status) {
    if (status == 'OK') {
      map.setCenter(results[0].geometry.location);
      var marker = new google.maps.Marker({
          map: map,
          position: results[0].geometry.location
      });
      // 下の1行を追加
      display.textContent = "検索結果：" + results[ 0 ].geometry.location
    } else {
      alert('該当する結果がありませんでした：' + status);
    }
  });
}
</script>
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAyezL_ScHfYIA3dFodDD0-9X1uBWGX9do&callback=initMap" async defer></script>

<p>マーカーのある位置の<br>
緯度 <input type="text" id="lat" value=""><br>
経度 <input type="text" id="lng" value=""><br>
地図上をクリックするとマーカーを移動できます。</p>

<div class="container">
 <div class="row">
  <div class="col">

<% @posts.each do |post| %>
<div>
  <p>投稿No.<%= post.id %></p>
  <%= link_to post_path(post.id) do %>
   <%= attachment_image_tag post, :image %>
  <% end %>
  <p>
    タイトル：
    <%= link_to post_path(post.id) do %>
     <%= post.title %>
    <% end %>
  </p>
  <p>コメント：<%= post.comment %></p>
  <p>ユーザーネーム：<%= post.user.user_name %></p>
  <p>投稿日時：<%= post.created_at.strftime('%Y/%m/%d %H:%M') %></p>
  <p><%= link_to "#{post.comments.count} コメント", post_path(post.id) %></p>
  <% if post.liked_by?(current_user) %>
    <p>
      <%= link_to post_likes_path(post), method: :delete do %>
        ♥<%= post.likes.count %> いいね
      <% end %>
    </p>
    <% else %>
    <p>
      <%= link_to post_likes_path(post), method: :post do %>
        ♡<%= post.likes.count %> いいね
      <% end %>
    </p>
  <% end %>
</div>
<% end %>

<%= paginate @posts %>

  </div>
 </div>
</div>